import Container from "../common/container";

const Header = () => {
  return (
    <div className="border-b fixed top-0 w-full">
      <Container className=" py-4">Header</Container>
    </div>
  );
};

export default Header;
